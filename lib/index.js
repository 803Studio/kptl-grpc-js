//
const company = require('./company')
const employee = require('./employee')
const global = require('./global')
const job = require('./job')
const users = require('./users')

module.exports = {
  company, employee, global, job, users
}
